interface:
  display_name: "中文需求文档编写"
  short_description: "按用户模板撰写中文需求，明确本次范围，按页面写清新增、调整及直接影响"
  default_prompt: "使用 $write-requirement-doc，参考我的模板和已确认要求，在本次任务范围内撰写或修订需求，按页面及业务顺序写清具体行为、必要关联影响和验收依据。"

policy:
  allow_implicit_invocation: true
